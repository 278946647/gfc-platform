# Copy to .env in repo root before: docker compose up -d --build
# (.env is gitignored — do not commit secrets)

GFC_BOOTSTRAP_TOKENS=demo-bootstrap
GFC_ADMIN_DEFAULT_PASSWORD=change-me-strong-password
GFC_AUTH_SECRET=change-me-random-long-string
